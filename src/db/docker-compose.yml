version: '3'

services:
  db:
    image: postgres:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER='postgres'
      - POSTGRES_PASSWORD=2921640
      - POSTGRES_DB='elite'
    volumes:
      - ./data:/var/lib/postgresql/data
